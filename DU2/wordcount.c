#include "htab.h"

int main(void) {

    // Create the hash table
    struct htab *test = htab_init(5);

    // While and load words into hash table
    // Print all words from hash table

    return 0;
}
